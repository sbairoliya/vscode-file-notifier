{
  "name": "vscode-file-notifier",
  "private": true,
  "scripts": {
    "install:all": "cd packages/local-notifier && npm install && cd ../file-watcher && npm install",
    "compile": "cd packages/local-notifier && npm run compile && cd ../file-watcher && npm run compile",
    "test": "cd packages/local-notifier && npm test && cd ../file-watcher && npm test",
    "package": "cd packages/local-notifier && npx vsce package && cd ../file-watcher && npx vsce package",
    "build": "npm run compile && npm run package"
  },
  "devDependencies": {
    "@vscode/vsce": "^2.22.0"
  }
}
